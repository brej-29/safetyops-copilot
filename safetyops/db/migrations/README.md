# Alembic Migrations

This folder is a minimal Alembic scaffold for future schema migrations.

- `env.py` is configured to use `safetyops.core.settings.Settings.database_url`.
- `versions/` should contain autogenerated migration scripts.

For v1, the database schema is created programmatically via `safetyops.db.session.init_db()`.