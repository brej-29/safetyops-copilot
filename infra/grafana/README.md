# Grafana (Placeholder)

This directory is reserved for Grafana dashboards and configuration.

For v1, Grafana is run with its default configuration via Docker Compose.
You can log in at `http://localhost:3000` (admin/admin by default) and
create dashboards that connect to the Prometheus instance defined in
`infra/prometheus/prometheus.yml`.